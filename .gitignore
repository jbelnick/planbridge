# dependencies & build output
node_modules/
dist/
*.tsbuildinfo

# test/coverage
coverage/

# runtime (PlanBridge writes config, audit log, and run state to ~/.planbridge)
.planbridge/

# env & secrets
.env
.env.*

# editor / OS
.DS_Store
*.log
